#include <stdio.h>

/**
 * main - Prints numbers from 0-9.
 *
 * Return: 0 on success
 * Description: print single digit numbers followed by a new line using only putchar
 */
int main(void)
{
  for (a = 0; a < b; a++)
    putchar(a);
  putchar("\n");
	return (0);
}
